# 验证记录与维护说明

> **本文件有两个用途**：① 记录这个 skill 的验证证据 ② **只在你要修改本 skill 时读**——工作文件里不写设计理由与自传。

> **这份文件证明什么、不证明什么**（先读这段，再读数字）：
>
> - **证明**：若干**具体行为**在应用本 skill 后发生了**预期方向的改变**——例如基线不写方案 / 不建文档 / 不澄清，而应用后停在了闸门、建了文档、提出了开放问题。
> - **不证明**：**整套流程稳定有效**。样本量小（4 轮对照 + 2 轮审查）、受试者是同一模型、**触发精度完全未验证**、跨模型稳定性未验证；且其中一次试跑**自身就没达到 skill 要求的文档完整性**（缺总纲与台账）。
> - **也不证明**：它在你的项目上划算。闸门与文档都是有价格的（见 `SKILL.md`「何时使用」的判据）。
>
> 判据：**把这份记录当"这些机制被观察到会起作用"，不要当"照做就能成功"。**

---

# 维护本 skill

> **本文件只在你要修改这个 skill 时读。** 工作文件（`SKILL.md` / `references/` / `templates/`）里不写这些——它们只写"做什么"，不写"为什么这么设计"。

## 写什么、不写什么（**本 skill 最重要的设计约束**）

**写纪律与工件，不写战术判断。**

- **该写**："**没有外部强制就不会做**"的事——冻结闸门、缺口表带"何时补"、验收只由命令输出填充、产物是线索不是证据。
- **不该写**：战术判断（该不该派子代理、怎么切分任务、要不要写脚本）。能力强的模型自己会算，**写死会反噬**——实测：给"隔离能省上下文"加了规则却没写成本门槛，下一轮受试者拿它处理一个 20 行探测就能解决的问题，耗时从 46 秒变成 25 分钟。

**加一条规则前先问**：如果没有这条，一个能干的 agent 会因为**不知道**而做错，还是因为**没被要求**而跳过？

- 前者是**知识** → 写进 `references/pitfalls.md`（一行即可），不要写成规则。
- 后者才是**纪律** → 值得写成规则。

**规则不能只住在本文件里。** 本文件是"改 skill 时才读"的，所以**任何规则只写在这里，等于没有**。第 11 轮那条「凡关于工具能力的断言，必须落到 `--help` 或实测」曾长期只活在本文件——直到一次真实构建里**同类缺陷造成了唯一的一次返工**，才被提到 `references/gates.md` 的规格自检（第四问）。本文件只保留它作为那次教训的记录。

**同一规则只允许一处权威，别处只引用、不复制清单。** 复制必然漂移——本 skill 反复踩：同一份数同时写成 3 份和 4 份、段数写成 6 和 7、旧规则在正文改了但副本没跟上。

**不在工作文件里写"我为什么这样设计"。** 设计理由、反噬故事、自传（"我犯过一次"）、自我评价（"最值钱的一处"）一律写在本文件——它们不改变使用者的行为，只稀释规则。

## 改完必须跑的核对（**常驻清单，逐项机械核对，不凭印象**）

> 这是本文件里**唯一一条对使用者的规则**——它是**维护者**的规则，而读本文件的人正好就是维护者，所以放在这里不算"规则只住在只读文件里"。
> 下面每条都必须**真的跑一遍**（能脚本化的就脚本化）。只靠"我这次注意了"必然漏——实测三次。

| # | 核对 | 期望 |
|---|---|---|
| 1 | **一处权威**：本轮新增/修改的每条规则，在全部工作文件里出现几处；**新写的段落里有没有把别处的清单又抄一遍**（清单是最容易被复制的东西——本 skill 反复踩的就是它） | **一处 + 指针**。多于一处 ⇒ 收成指针 |
| 2 | **纪律 / 工件，还是理由 / 战术**：逐段分类 | 只允许前两类；理由与战术**删掉**（理由搬本文件） |
| 3 | **悬空引用**：工作文件里出现的 `references/…` `templates/…` `scripts/…` 路径、以及 `[[节名]]` 式引用 | 全部存在。**只查工作文件**——本文件的轮次叙述会**合法地**提到已删除的文件（那是历史记录，不是活的引用） |
| 4 | **自述残留**：工作文件里有没有自指（"本 skill"）、自传（"我犯过一次"）、自我评价（"最值钱"）、设计论证 | **0** |
| 5 | **主循环图**：`SKILL.md` 的 dot 图 | 无悬空节点、全可达、**成功与失败出口都齐全** |
| 6 | **规则落点**：本轮新增的规则，有没有只写在本文件里 | 规则必须在工作文件里；本文件只留"为什么"与证据 |
| 7 | **驱动器语法**：改了 `scripts/` 就跑 `bash -n` | 通过 |

> 第 1、2 两项是第十三轮加的；第 6 项是第十四轮加的。**加核对项本身也要走这套清单**——否则新加的那项又会只活在某一轮的叙述里。

## 换工具 / 换语言时要改什么

**协议与工具无关**：任何有非交互模式的 agent CLI 都能当工人。`scripts/` 里是一份 omp 实现，不是协议本身。要改两处：

1. 驱动器里的 `AGENT_CMD` 与参数名。**必须保留的语义**：非交互单轮 / 可续接同一会话 / 工作目录可指定 / 自动批准 / 有运行上限 / **结局写进文件** / **收到信号时终止工人**。
   **两个必填环境变量**：`RECORD_DIR` 与 `WORK_DIR`——**不要给默认值**，猜错会让工人在错误目录里干活，且失败现场看起来像"工人崩了"。
2. 窗口配置叠加层的字段名。**必须保留的意图**：上下文将满时自动落盘一份交接文档。

换语言时还要改：`templates/AGENTS.md §5` 的门禁命令、`references/pitfalls.md` 里对应语言的段落。

## 本 skill 的已知局限

1. **架构者没有独立对手。** 流程把**设计规格、派活、判分**交给同一个人——那个人有系统性盲区，且自己看不见。缓解做法（可选）见 `references/delegation.md`「独立对抗读者」。
2. **触发精度未验证**（见下）。
3. **架构者自己的上下文没有自动压缩点**——缓解办法见 `references/verification.md` 末节。

## 独立对抗读者为什么没设成硬闸门

它有成本（读全文 + 复核意见），而**本 skill 刚因为"无门槛地推荐隔离"反噬过一次**。什么时候值得付，取决于交付物的重要性，那是战术判断——**按上面的设计约束，战术判断不该写死**。所以只作为可选做法。

---

> 本机规范要求：**没有失败的测试就不写技能**。本文件是创建与历次修订时的红绿证据。
> 方法：同一场景、两个受试 agent、各自独立目录；唯一区别是其中一个被要求先读本 skill。证据取自**产物文件**、**结构化报告**与**我自己复跑的命令**，不采信自述。
>
> **本文件曾含逐轮完整转录（303 行），已压缩为"结论 + 证据类型 + 据此的修订"。** 压缩掉的是一次性细节（具体 sha、输出片段、场景字段表），保留的是可复核的判据。

## 七轮一览

| 轮 | 内容 | 谁错 | 结论 |
|---|---|---|---|
| 1–2 | 对照测试（CSV 去重小工具，带时间压力） | 基线 | skill 让行为变好：基线**零文档、不澄清、不停闸门**；应用后停在闸门 A、建了文档、提了开放问题 |
| 3 | 小问题（20 行探测） | **skill** | **过度套用**：46 秒 → 约 25 分钟，且受试者**既派活又自己做了一遍** |
| 4 | "不可压缩"大语料（65 文件 / 389 KB / 97k tokens / md5 全不同 / 十种语法） | **我的语料设计** | **假设被证伪**：两路各自独立写脚本压成 64 行表 ⇒ **本地语料几乎总能压掉** |
| 5 | 决定不做（外部世界隔离那一面） | — | 可从原理推导，不单独验证；**已就地标注为"推论、非实测"** |
| 6 | 两位审查者逐行读 16 文件（自洽性 / 可执行性） | **skill** | 40 条意见 → 去重 25 条，**全部为真**；我自检只找到 2 条 |
| 7 | 用户本人 6 条 | **skill** | 全部成立；诊断：**规定太多、权威位置不够集中、部分超出证据** |

## 关键证据（每轮一条，够复核即可）

**1–2 轮**：基线产物 = `csvdedup.py` + `test_csvdedup.py` + 冒烟夹具，**零文档**，且**一次都没向用户澄清**（全靠假设默认值）。应用 skill 后：8 段齐全的冻结方案、工人合同、缺口表 6 项**每项都写到函数级的"何时补"**、派 1 个子代理、自写 20 条对抗性反例。

> **关键发现**：基线的**后置验证其实很好**（19 个测试 + 20 万行冒烟 + 三条 IO 路径逐字节比对）。它缺的是**前置**——方案审查、向用户澄清、可交接的文档。⇒ 本 skill 治的是**前置缺失**，不是"工人不好好测"。
>
> **未达标项（如实记录）**：lite 档要求「总纲 / 缺口 / 验收 / 台账 / 交接」，实际产出缺**总纲**与**台账**。⇒ 结论收窄为「闸门与验收**行为**达标；**文档完整性未达标**」。

**最值钱的一次产出**：受试者自写的对抗性反例抓到工人测试漏掉的真 bug——不变量「绝不修改输入文件」在 `--force -o <输入自身>` 下被违反（rc=0，输入 sha 改变）。退回修复后 20/20 反例全过。**我用不同夹具独立复现了同一结论**（两边夹具不同却得到同一判定，比"用同一夹具复现"更有说服力）。

**3 轮**：基线与 treatment 的耗时比约 30 倍。
> **⚠ 归因限制**：两路**不同工**——treatment 必须先读完 skill 的 references + templates。所以这个倍数**包含读 skill 的固定开销**，不能全归因于"隔离型派活"。干净可归因的只有：treatment **既派活又自己跑了一遍**（两份成本）+ 派活本身的等待与复核开销。第四轮提供了一条"读了 skill 但没派活"的参照，倍数 **4 倍**，更接近纯流程开销。

**4 轮**：我给测试预置的答案键（8 处异常）**被两路受试者各自独立反驳**。我自己写 AST 脚本重算后确认**它们对、我错**——根因是我的语料生成器有 bug（算术形式生成的值不等于预设 `bound`）。**与第 2 轮同源**：那一次是我的规格（掩码幂等）自相矛盾。**两次都是工人揪出来的。**

**6 轮**：25 条里有 **8 条是"清单/规则散落在多处且已漂移"**——skill 自己反复警告的那条，**对别人有效、对我自己没生效**。最严重的几条：`SKILL.md` 仍留着第三轮被证伪的无门槛规则（**修正没传播到"每次必读"的入口**）；`pitfalls.md` 把第四轮的 4 倍耗时当成"隔离比写脚本贵"的实测（**归因错误**——那一轮两路都没派活）；`VALIDATION.md` 的证据栏填的是受试者自述（**判分依据与被判对象同源**）。

**7 轮**：用户逐条给出文件与行号，6 条全部成立且比所述更广（`六段式` 有 5 处残留；`journal.md` 标题写"九份"而表里已 10 行）。据此的修订：**每条规则只在一处定义，别处只引用**；闸门 A 分授权/未授权两形态；工人合同模板分【通用】/【示例】；验收数字改为随规模与风险调整；脚本声明改为准确表述；验证材料加作用域声明。

## 元结论

**四轮测试的元结论：两轮是 skill 错，而两次错都源于同一个动作——我往 skill 里加战术判断。**

| 轮 | 谁错 | 原因 |
|---|---|---|
| 1–2 | 基线错 | **不是笨**，是**没有任何东西要求它做** |
| 3 | **skill 错** | 我只写了"隔离能省上下文"，没写"何时不值得" |
| 4 | **我的语料设计错**；模型做对 | 模型自己找到了脚本捷径 |

⇒ **写纪律与工件，不写战术判断**（判据见 `SKILL.md`）。

**第六轮的元结论**：25 条里**没有一条**是我自己自检能发现的。**审查必须交给对抗性的、不同视角的读者**——而且两份不同视角 > 一份全面（A 找到结构性问题，B 找到可执行性问题，交集很小）。

**第七轮的元结论**：**"规定太多"本身就是缺陷**——它制造漂移面、超出证据、并让执行者不知道以哪处为准。**加规则前先问两句**：这条有没有**更权威的落点**？它有没有**证据支持**？

## 第八轮：**用户的减法指令**（skill 结构性臃肿）

用户读完 skill 后的判断：**"一点都没有做减法的决心和大刀阔斧完善 skill 的意识"**。

**实测后确认这是对的，而且比"规定太多"更准确**——问题不是规则本身太多，是**同一规定被写了 3–5 遍，且副本之间已经漂移**：

| 内容 | 写了几处 | 漂移证据 |
|---|---|---|
| 切片指令节次 | **3** | `delegation.md` 自己写着"本文件不重复列"，**紧接着列了全部 7 节** |
| 交接文档节次 | **3** | delegation 列全 + `templates/handoff.md` + `prompt-slice.md` 内嵌 |
| 目录布局 | **2，两处都自称"唯一定义"** | 直接矛盾 |
| 四级闸门工件 | 2 | SKILL.md 快查表 + gates.md 详表 |
| 验收五步 | 3 | acceptance.md 仍写"抽 **2 个**"，与第七轮新规则冲突 |
| 文档份数 | 3 | **SKILL.md 说 9 份，`journal.md` 表里 10 行**——第七轮声称修好，**实际没修** |
| 旧规则副本 | 4 处未跟上正文 | SKILL.md **主循环 dot 图里仍写着 `否（≤2 次）`、`是/无回复按最小档位`**；常见错误表、pitfalls、acceptance 同样 |

**据此的减法**（16 文件 / 1760 行 / 101,827 B → **12 文件 / 1035 行 / 64,798 B**）：

| 动作 | 理由 |
|---|---|
| 删 `README.md` | 导航表是"文件清单的副本"；其余并进 SKILL.md / delegation.md |
| 删 `references/journal.md` | 并进 `gates.md`「记录系统」 |
| 删 `templates/handoff.md` | `prompt-slice.md` 已内嵌完整交接模板——留一处 |
| 删 `templates/journal-docs.md`（**266 行，全 skill 最大**） | 其中大部分是**真实运行的示例产物**，不是格式。格式压成「每份文档必含的节」一张表 |
| `VALIDATION.md` 303 → 68 行 | 只留作用域 + 七轮结论 + 关键证据；逐轮转录压缩 |
| 删 `gates.md`「七条机制」 | **7 条里有 5 条是别处的复述**（缺口带何时补 / 验收只能命令输出填充 / 决策含备选代价 / 文档代码分开提交 / 台账记可靠性） |
| 删 `pitfalls.md` 里 2 条复述验收规则的"陷阱" | 它们是纪律的重述，不是意外——并给陷阱库加了入库判据："**收的是意外，不是纪律的重述**" |
| 全文件清掉计数写法与旧规则副本 | 计数本身就是漂移源 |

**SKILL.md 重写为以「三条纪律」开头**——核心原则先给，四级闸门降为一张表。读者 10 秒内能拿到本质，而不是先读四层闸门。

**这一轮抓到的自伤**：重写 SKILL.md 时我把档位表删了、改成指向 `freeze-plan §0`，而 `freeze-plan §0` 原本指向"SKILL.md 的档位表"——**制造了一个循环引用，两边都没定义**。改用脚本逐一核对所有"唯一定义"声明后才暴露。**这正是第七轮那条教训的实例：删副本时必须同时确认权威落点还在。**

**这一轮的教训**：**"多写一遍"看起来是冗余、实际是漂移源。** 加内容前先问"这条已经在哪说过"，删内容后必须**机械核对**（路径、节名、权威声明），不能凭印象。

## 第九轮：**四个互不相交视角的对抗审查**

派 4 个 agent 逐行读完全部 12 文件，视角互不相交：**前提质疑 / 首次使用者干跑 / 领域对照（缺什么）/ 反噬面（什么时候害人）**。合计约 40 条，去重后 **31 条**，我逐条复核——**全部为真**。

**这一轮的元发现（四个视角里有三个独立命中同一条）**：

> **`docs/<契约>.md` 是工人必读的"四样"之一，被声明为"实现与契约冲突时先改文档再改代码"的契约——但全库没有任何步骤产生它。**
> 冻结方案 9 节没有它；记录系统 10 份文档没有它；`docs/` 布局里**预留了"仓库内契约"这个位置却没有任何东西填它**。
> 派生症状：切片指令的必填项 `<本轮要守的不变量编号>` **填不出来**——不变量没有权威落点。

**用户对此的判断**（决定了修法方向）：*"本 skill 既可以用于从零构建也可以用于重构，所以需要看情况；而且就算是重构，也需要乘着重构的机会，合理大胆地完善老项目的契约。"*
⇒ 契约来源分两种模式，且**重构不是照抄旧行为，是逐条裁决（保留 / 修正 / 废弃）+ 理由**。

**31 条按性质分三类：**

| 类 | 内容 |
|---|---|
| **结构性空洞 6 条** | ① 契约无生产者 ② 没有任何累积回归门禁（工人只跑包限定检查，全量只在最后跑一次）③ 提交策略缺失且 `.git` 未被列为共享可变资源 ④ 没有中止路径与预算断路器 ⑤ 架构者自己没有任何上下文压缩点（而流程精心管理了工人的）⑥ 流程里没有独立对抗读者，而 skill 自己的元结论说那是最有价值的一环 |
| **可验证缺陷 13 条** | `--add-dir` 把整个记录目录授权给工人（与"工人不得读 `doc/`"方向相反）；兜底 handoff 搬运是死代码（**实测确认**）；`trap` 只写标记不终止工人；档位 size-based 准入 vs cost-based 使用判据冲突；"档位必须共定"与"通报形态"互斥；`AGENTS.md` 把「单元测试与被测模块同文件」标为【通用】（**Rust-ism**）；主循环图漏掉"规格自检"、没有失败出口、把闸门 B 路由回"通报即可"；`verification.md` 与 `acceptance.md` 的抽查数量副本漂移；闸门 B 的 ADR 落在工人读不到的 `doc/`；缺口"何时补"逼永久缺口编假日期；description 的排除判据与正文相反 |
| **该删/降级 4 条** | `pitfalls.md` 委托类整表复述别处规则；**闸门 C 不是闸门**（"默认不问用户"的东西与三个真闸门并列）；范围变更史三处权威；台账可靠性栏只写不读 |

**两处我未能验证、如实标注的**：

- **「`trap` 在外部 kill 时不执行」**：审查者称 Git Bash 实测不触发。我自己尝试复现时环境不可靠（bash 工具跑的是 **WSL2**，`pgrep` 看不到后台作业 ⇒ 我的 `pkill` 从未送达信号）——**我的失败结论已撤回**。但 bash 文档明确"等待前台命令时不执行 trap、且不转发信号给子进程"，且**读码可确认 trap 从不 kill 子进程**。⇒ 采信"**不终止工人**"（可静态确认），不采信"完全不触发"（未验证）。
- **兜底 handoff 的精确文件名/深度**：真实运行里**全盘没有任何 `handoff-*` 文件**（说明这段兜底从未生效），只能确认 `.sessions/<W>/` 下有 `<session-id>/` 子目录这一层。⇒ 修法覆盖两种深度，而不是精确匹配某一层。

**这一轮的教训（比逐条问题更重要）**：

> **前八轮我一直在改"规则写错了"，这一轮暴露的是"规则覆盖不到的地方"。**
> 前八轮的审查对象是**文本**（矛盾、重复、措辞）；这一轮四个视角里有三个是**角色扮演**（我照着做会卡在哪 / 少了什么 / 什么时候会害人），命中的全是文本审查看不见的东西。
> ⇒ **审查要换角色，不只是换视角。**"你觉得这份文档怎么样"和"你来用它做一件真事"是两个不同的问题，后者有效得多。

## 第十轮：**用户指出"战术判断"又混回来了**

用户读第九轮的改动后，指着 `delegation.md` 新加的「**为什么不设成硬闸门**」问：*"这个有必要写吗？你是不是又忘记我对于 skill 的要求了？通读全文看看有什么这样的问题。"*

**成立，而且是同一类病。** 通读后清出的全部同类段落：

| 位置 | 内容 | 性质 |
|---|---|---|
| `delegation.md` | 「为什么不设成硬闸门」+「判据（如果你要决定派不派）」 | **为"该不该设闸门"写论证**——用户举的例子 |
| `delegation.md` | 「结论：分工，不是替代」+ 两条 | **战术判断**——而设计约束明写"不该写该不该派子代理" |
| `delegation.md` | 「切片指令：为什么是这几节」整表 | 模板的设计理由 |
| `delegation.md` | 「换工具时只需适配两处」「协议与工具无关」 | 维护者指南 |
| `SKILL.md` | 「写什么、不写什么」整节 +「加一条规则前先问」 | 维护者指南 |
| `SKILL.md` | 「本 skill 的两处已知局限」整节 | 关于 skill 的声明 |
| 各处 | 「本 skill 曾写错这一条」「本 skill 自己反复踩」「我犯过一次」「这是缺陷」「最值钱的一处」「这是本 skill 里最省不得的一步」 | 自传 / 自我评价 |
| `gates.md` | 「这一关不是"批准"…和 A/B/D 不是一类东西」 | 改名后的命名辩护 |

**结构诊断（比逐句删更重要）**：**工作文件（"做什么"）和自述文件（"我为什么这么设计、我犯过什么错、证据来自哪次测试"）混在一起。**

**修法不是逐句删，是分开**：

- 自述全部收进 `VALIDATION.md`（新设「维护本 skill」节，并声明它的双重用途）——它本来就是"改 skill 时才读"的文件。
- 工作文件只留**规则、工件、以及"不做会怎样"**（行为后果改变行为）。
- **保留**证据作用域标注（"未做对照验证"）——它不是自传，是改变读者信任度的标记。

**判据（写下来供以后用）**：

> 删「我为什么这样设计 / 我犯过什么错 / 证据来自哪次测试」；留「不做会怎样」。

**这一轮的教训**：第九轮我修了 31 条**内容**问题，但**又新增了一整节设计论证**——说明"不写战术判断"这条约束**知道不等于会遵守**。它需要被放进维护清单，并在每次改完后机械核对（本轮的清点表就是这么得出来的）。

## 第十一轮：**用户的 6 条 + 1 条可选边界**

用户逐条给了行号与替代措辞。**6 条全部成立**，其中两条指出第十轮的"修法"是**表面功夫**：

| # | 用户指出 | 我上一轮错在哪 |
|---|---|---|
| 1 | 统一授权措辞（纪律 1 与闸门 A 的两种形态自相矛盾）；**B 只让影响承诺/成本/权限的变更决定**；**D 是交付结果，不默认再等一次批准** | 纪律 1 仍写"让用户批准"，与后面的两形态冲突；B/D 都标"必须问用户"，把**汇报**当成了**批准** |
| 2 | 删掉工人合同的「与代码冲突时以代码为准」；契约是行为标准，冲突先报告、由协调者裁决；**工人不得为使现有代码通过而自行改契约** | 我写的是"先改文档再改代码"——**仍然允许工人自行改契约**，只是换了个方向。原句更是让工人信代码 |
| 3 | 缺口分**阻断 / 可接受**；未满足当前验收标准的阻断"完成"；**有阻断项时只能报"部分完成"，即使每项都写了"何时补"** | 第十轮我让"永久接受 + 理由"成为"何时补"的**等价替代**——于是任何缺口都能被一句理由放行，"完成"退化成填表 |
| 4 | 验收要覆盖**全部 Git 状态**（提交 + 暂存 + 未暂存），并与工人交接的 SHA/路径核对；**共享分支上 `基线..HEAD` 不等同于某一工人的产物** | 我只加了"记基线 SHA"——但共享分支上那个区间**混着别的窗口的提交**，根本没有归因能力 |
| 5 | **`--add-dir` 不会隔离已在工作目录内的文件**；驱动器要区分"收到信号 / 已发终止 / 确认退出"，**不要在尝试终止之前写出看似最终的结局**，也不要保证子进程树一定停止 | 我写的"驱动器只把 `handoffs/` 加进工人的工作区"**暗示了一个不存在的隔离**——`omp --help` 原文是 *Add a workspace directory*，**只增不限**；而 `doc/` 就在工作目录里。trap 也确实是先写 `EXIT=driver-killed` 再 kill |
| 6（可选） | 重写旧系统时，"工人不得读旧实现"是**有代价的隔离模式**，应仅在明确采用时启用；其他情况允许**只读取旧实现取证**，但仍以契约为实现标准 | 我把"禁止读任何参考实现"标成了【通用】——它对重写是**默认有害**的（契约不完整时工人连取证都不能） |

**据此的修订**：

| # | 修订 |
|---|---|
| 1 | 纪律 1 改为"已有实现授权则通报并推进；缺少授权或涉及未授权的不可逆操作才停下等批准"；闸门表的列名从"问用户？"改为"**要用户决定吗？**"；B 收窄到承诺/成本/权限；D 明确为**交付**（含"完成 / 部分完成"判定），不设等待 |
| 2 | 工人合同改为：**契约是实现标准**；代码/合同/契约冲突**先报告、由架构者裁决**；**工人不得为使现有代码通过而自行改契约** |
| 3 | 完成定义改为「没有阻断项」；缺口表加 **"阻断？"** 列；阻断项存在时只能报「**部分完成**」 |
| 4 | 台账记**基线状态**（SHA + `git status` + 已有差异）而非只记 SHA；验收第 2 步查**提交 / 已暂存 / 未暂存**三样并与工人交接的 SHA、路径逐条核对；验收模板同步 |
| 5 | 记录系统删掉虚假的隔离声明，改为如实说明（`--add-dir` 只增不限；唯一作用力是合同约定 + 验收时核对越界读）；驱动器 trap 改为**三步记录**（SIGNAL-RECEIVED / TERM-SENT / KILL-SENT + `WORKER=terminated|still-alive`），并声明**不保证子进程树停止** |
| 6 | 工人合同 §1 改为**两种模式**：默认允许只读取旧实现取证（**证据不是标准**，契约说了算）；严格隔离需本轮指令明确要求，并注明其代价 |

**这一轮的教训**：

> **"修了"和"修对了"是两件事。** 第 5 条尤其明显：我上一轮把一个错误的声明改成了另一个**仍然错误**的声明（把"没有隔离"写成"驱动器做了隔离"），而且**没有去核实工具的实际语义**就写了结论。用户一句"`--add-dir` 不会隔离已在工作目录内的文件"就戳穿了——他查的是 `omp --help` 原文。
> ⇒ **凡是关于工具能力的断言，必须落到 `--help` 或实测，不能凭意图推断。**

## 第十二轮：**旧实现可见性默认值反转**

用户修正第十一轮的第 6 条：*"重写旧系统时，工人不得读旧实现是有代价的隔离模式。**默认不许读旧项目，想读的话需要问一下。**"*

**从"默认允许取证"改为"默认禁止 + 申请"。** 这个方向更贴合本流程：**申请本身会留下记录，而且申请内容直接暴露契约的缺口**——架构者据此**补契约**，而不是逐案放行。

| 位置 | 改动 |
|---|---|
| `templates/AGENTS.md §1` | 删掉"两种模式"，改为：**默认禁止读**；代价由架构者承担不由工人承担；需要读时**攒成一份申请**（要读什么 / 为什么要读 / 不读的后果），架构者裁决「允许读（划范围）/ 改补契约 / 明确按最小做法」；指令里预授权的范围可直接读 |
| `templates/prompt-slice.md` | 声明默认禁止，并留**预先授权只读范围**的槽位 |
| `templates/freeze-plan.md §3` | 登记预先授权范围；未预授权的走申请 |
| `SKILL.md`「契约规格」 | 加一句：默认禁止读旧实现；**契约的完整性由我兜底**，工人的申请就是契约缺口的信号 |
| `references/delegation.md` | 「让工人产出可信的信号」加一行：**申请读旧实现而不是偷看** |
| `references/pitfalls.md` | 加一条：默认允许读旧实现 ⇒ 产出继承旧妥协，且事后分不清哪条是契约规定的、哪条是抄来的 |

**一个刻意的设计点**：申请要**攒成一份**、不要逐条问——否则每次契约缺口都要一个来回。这样把"隔离的代价"从**每项一轮**压到**每切片一轮**。

**这一轮的教训**：上一轮我把"默认允许"当成对"严格隔离"的松绑，但**默认值本身就是一道闸门**——默认允许等于这道闸门不存在。用户改成默认禁止后，"是否读旧实现"才真正变成一次**要留痕的裁决**。

## 第十三轮：**用户指出 e5f3d4a 违反了写入约束**

用户指着第十二轮的提交问：*"你是不是忘记了 skill 应该写什么。"*

对着 skill 自己的约束（「写纪律与工件，不写战术判断」；「删『我为什么这样设计』，留『不做会怎样』」）复核那个提交——**三处违规，方向一致**：

| 位置 | 写了什么 | 为什么违规 |
|---|---|---|
| `templates/AGENTS.md §1` | 「这是有代价的隔离…**这个代价由架构者承担，不由你承担**」 | 设计理由 + 责任归属——**工人不需要知道代价由谁承担** |
| `templates/AGENTS.md §1` | 「架构者会裁决：允许读 / 改补契约 / 明确按最小做法」 | **架构者的决策菜单写进了工人合同**——工人只需知道"要申请" |
| `templates/AGENTS.md §1` | 「（不要逐条问，那会来回很多轮）」 | 效率战术 + 它的理由 |
| **六处** | 同一条规则写在 `SKILL.md` / `AGENTS.md` / `prompt-slice` / `freeze-plan` / `delegation` / `pitfalls` | 违反第十二轮**刚立的**「一处权威」 |

**根因**：`templates/AGENTS.md` 是**给工人的合同**，不是给架构者的操作手册。我把"我会怎么裁决"和"这么设计的代价"一起写了进去——**那两样都不改变工人的行为**。

**修法**：工人侧只留「规则 + 申请这个工件 + 例外条件」；架构者的决策菜单只留在 `SKILL.md`（那是架构者的文档）；其余四处收成指针。

**这一轮的教训**：

> **「一处权威」与「不写战术判断」这两条，知道不等于会遵守。** 第十二轮刚立的规矩，第十三轮自己就破了——因为我把注意力放在"用户要什么"上，**没有在写完之后对着约束复核一遍**。
>
> ⇒ 机械核对新增两项（第 1、2 项）。**清单已提到本文件的「改完必须跑的核对」节**——本节只留这条教训，不持有清单。

## 第十四轮：**用一次真实构建反过来审 skill**（本轮）

前三轮是"审 skill 自己"。这一轮换了来源：**先做完一个真实委托式构建，再拿它的证据回头审 skill**。

**来源项目**：把一个 5696 行的 Go 工具（GUI + CLI，Win32 原生界面）重写成 Rust，只做 CLI。9 个工人窗口、9 个切片、38 次提交、121 个测试。

**最有价值的观测**：**工人上报的 9 处规格缺陷，全部是我写坏的，没有一处是工人的错**。其中只有 1 处导致了返工——而它的形态很具体：

> 契约里写「PNG 等级 1–3 输出**索引格式**」——那是**旧实现的行为**，我没有核实**所用的库有没有索引编码路径**。它没有。

同类还有一处（我凭 crate 简介断言某库含某算法，实际不含），被工人在动工前拦住。⇒ **两处同一根因**，一处付了返工代价。

**据此的 7 处修订**（每条都过了「纪律/工件 vs 战术」与「一处权威」两关）：

| # | 改哪 | 性质 | 依据 |
|---|---|---|---|
| A | `gates.md` 规格自检加**第四问**（库/工具能力必须核实）；并在本文件的维护节立"规则不能只住在这里" | **纪律** | 唯一导致返工的缺陷类 |
| F | `verification.md` 反例设计加一条**判据**：照行为重写的算法，"够"的标准是**中间量逐字段对得上**，不是端到端输出像 | **判据** | 一个复刻算法带着 22 个全绿测试通过，而中间量错了一处（乘积溢出饱和），只有逐字段比对查得出 |
| C | `delegation.md` 共享可变资源补一类：**模块/依赖声明的入口文件** | **清单条目** | 并发窗口各追加一行声明 ⇒ 后提交的基于过期读取，**静默回退**先提交的那行 |
| B | **修正** `delegation.md`「同包必须串行」——过严 | **修正** | 同一 crate 里三窗口并行、文件集不相交，第 1 轮全过 |
| E | `gates.md` 闸门 C 补：**已验收产物被后续决策改动**时的处置（自己改 + 重跑全量 + 追加修订节） | **纪律** | 触发两次；不写就会覆盖验收记录、或白开一个窗口 |
| D | `templates/freeze-plan.md` 断路器补：**上限必须高于切片数** | **判据** | 我把上限设成等于切片数，第一次返工就触顶 |
| H | `gates.md` 把"工人不得读 `doc/` 其余部分"从声明补成**可执行核对**（grep 标题串，命中应为 0） | **工件** | 每轮都跑，命中全为 0 |

**未采纳的**（够格但判断为不该写）：

- **"照行为重写算法时，把参考实现编成程序比对"的具体做法**——它是**战术**，能力强的模型自己会想到（实测里工人主动做了）。只留**判据**（中间量对得上），不留**做法**。写死做法会反噬：一个 20 行的算法不该被要求去编一个参考程序。
- **"并行波次被挡住时把目录复制到临时 crate"**——是**对策**不是纪律 ⇒ 进 `pitfalls.md`，不进 `delegation.md` 的规则表。

**这一轮的教训**：

> **skill 的缺陷不在"想得不够多"，而在"规则的落点错了"。** 最贵的那条规则（能力断言必须实测）**本来就存在**，但它住在 `VALIDATION.md`——一个"改 skill 时才读"的文件。它没有失效，是**没人在干活时读到它**。
>
> ⇒ 新增的元规则：**规则不能只住在 `VALIDATION.md` 里**；只写在这里等于没有。本文件只留"为什么这么设计"与证据。

## 第十五轮：**用户问"刚才有人读了吗"——查出是我授权的越界读**

用户一句 *"工人不得读 doc/ 的其余部分 刚才有人读了吗"*。查 transcript（`.sessions/*.jsonl` 的工具调用）：

**4 处真的读了 `doc/01-决策记录.md`**（W7 一次、W8 两次、W9 一次）。**全部是我在指令的「必读」里写进去的**——W1–W6 的指令 0 处提及，也就 0 处越界。

**两处教训：**

**① 我刚立的核对方法抓不到它。**

| 方法 | 结果 |
|---|---|
| 查**流水 + 交接的文本**（第十四轮刚写进 `gates.md` 的方法） | 命中 **0**——抓不到 |
| 查 **transcript 的工具调用** | 命中 **4**——抓得到 |

读了却从不提它，**文本层没有痕迹**。⇒ 核对方法已升级为查 transcript。

**② 越界的成因是我漏了规则的另一半。**

`gates.md` 的闸门 B 早就写着「ADR 在 `doc/` 里，工人读不到 ⇒ 只写 ADR 等于没通知工人」。我照做了（把 ADR-0006 的约束镜像进契约的 `I7` / `§3` / `§4.7` / `D22`）——**但同时还把 ADR 写进了必读**。镜像之后指 ADR 是**纯多余**，且**制造了第二处权威**（会漂移）。

⇒ `gates.md` 补上那另一半：**镜像之后也不要让工人去读 ADR**。

**这一轮的教训**：

> **"报告了"与"核对过"是两件事。** 第十四轮我写下核对方法时，**没有拿它去跑一遍已知的违规**——于是把"文本层可见"当成了"可见"。规则要配一个**能真正判定的检查**，而那个检查必须**拿已知违规试过**。

## 全部未覆盖的测试面（如实声明）

- **触发精度未验证**：受试者都是被**明确要求**（或未被告知）读 skill 的，因此只验证了"应用后行为改变"，**没有验证 description 能否把该用的任务勾进来、把不该用的排除掉**。这是当前唯一完全未验证的关键面，且**无法靠推理替代**。
- **规格自检未做对照验证**：`references/gates.md` 的四问（可判定 / 无冲突 / 签名与散文一致 / 库与工具能力已核实）已就地标注为"推论、非实测"。其中第四问的**依据是实测的**（一次真实构建里它拦不住的那处造成了返工），但"加了这一问之后能拦住"**未做对照验证**。
- **"外部世界该隔离"未单独验证**：从原理推导，已就地标注。
- 受试者是**同一模型**，未验证跨模型稳定性。
- 记录目录**未做 `git init`**：真实那次运行里"文档与代码分开提交"这条纪律全程空转。
