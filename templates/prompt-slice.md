# 切片指令模板

> **本文件是两件事的唯一定义**：① 切片指令的节次 ② 交接文档的节次（在文末）。别处只引用，不复制清单——复制必然漂移。
> **规格自包含**：工人只读哪几样见 `SKILL.md` 三条纪律。
> 判据：如果这条指令里出现了"去参考一下那个项目"，说明我还没想清楚。

---

# <工人 id> · <切片名>

## 角色

你是 `<项目名>` 的 `<语言>` 实现者。`<一句话项目定位>`。**你的工人 id 是 `<id>`**——我会用它取你的产出、审计你的过程、并在需要时续接你。

工作区现状：`<已完成的切片>`；`<本轮涉及的模块>` 还是空占位。

本轮实现 **<切片名>**：<目标一句话>。

## 必读

- 仓库根 `<AGENTS.md>`（已自动加载）——特别是 §<相关小节>。
- 仓库内契约 `docs/<契约>.md`——**<本轮要守的不变量编号> 是本轮全部内容**（契约没覆盖的细节按最小做法决定并写进交接文档，别猜）。
- 上一轮交接：`orch-doc/handoffs/<上一轮工人 id>.md`（**`orch-doc/` 下只允许读这一个子目录**）。
- 本指令。

**旧实现的可见性**：默认禁止读。本轮**预先授权**的只读范围：`<路径，或"无">`——未列出的不许读，不要申请。

## 约束

- **当前范围与非目标**：<从 `docs/freeze-plan.md` 的当前有效版本摘出与本片相关的边界，逐条列明；没有相关非目标则写「无」>。
- 只改 `<目录>/**`（manifest 已就绪，**不要加依赖**）。
- 遵守 `<语言/项目对应的硬约束>`（如禁止项、lint 列表）。
- 所有公开项必须有文档注释。
- <本轮的专属约束，例如"不得写磁盘、不得碰数据库">

## 交付物

### 1. `<文件路径>`

```<语言>
<接口签名或数据结构定义——精确到字段名与类型>
```

**规则**（逐条，编号，可判定）：

1. <……>
2. <……>

**测试要求**（表驱动，逐条列出必测场景）：

| 主题 | 必测 |
|---|---|
| <……> | <……> |

### 2. `<文件路径>`

<同上>

## 验收（自己跑通并把**真实输出**贴进交接文档）

```bash
<限定到本包的检查命令：格式化 / 静态检查 / 测试>
```

（用包限定参数：同一时刻可能有其它工人在别的包工作。）

## 禁止

- 不改 `<目录>` 之外的任何文件。
- 不新增依赖。
- 不用抑制注解掩盖警告（测试模块除外，需注明）。
- 不用批量暂存；提交时显式列出路径。
- **不要编造任何命令输出。**

## 收尾

1. 写 `orch-doc/handoffs/<工人 id>.md`（模板见下）。「关键选择」必须写明：<本轮最需要解释的 2–3 个判断点>。
2. 提交（显式路径）：

```bash
git add <逐个文件路径>
git commit -m "<type>(<scope>): <summary>"
```

> **本轮结束后你的会话会被保留**（`idle` → `parked`），我可能发消息向你追问，让你在同一会话里续轮——**不要假设这次结束就完事了**。交接文档要写到"换一个人接手也能继续"的程度。

## 交接文档模板

```markdown
# <工人 id> 交接文档
## 切片
<切片名>   状态：完成 / 部分完成 / 阻塞
## 本轮改动的文件
- `path` — 一句话
## 本轮提交
`<sha>` — `<subject>`（逐个列出；验收要用 `git log <基线>..HEAD` 核对这些）
## 当前状态（可复现）
命令 + 真实输出摘要（贴关键行，含测试数量）
## 我做出的关键选择
## 未完成 / 已知问题
## 下一轮建议的第一步
## 陷阱
```
